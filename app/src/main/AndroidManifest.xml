<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Adaptere.ComenziAdapter" />
        <activity android:name=".FormularAdaugare.ComandaActivity" />
        <activity android:name=".FormularAdaugare.ComandaProdusActivity" />
        <activity android:name=".Grafic.GrafPretActivity" />
        <activity android:name=".Logare.InregistrareActivity" />
        <activity android:name=".ActivityMenu" />
        <activity
            android:name=".MeniuSecundar"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Logare.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>